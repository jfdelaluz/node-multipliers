# Notes:

```
Options:
      --help       Show help                                           [boolean]
      --version    Show version number                                 [boolean]
  -b, --base       Base multiplier                           [number] [required]
  -l, --list       Display multiplier results         [boolean] [default: false]
  -m, --multiples  Multiples to use against multiplier    [number] [default: 10]
```
